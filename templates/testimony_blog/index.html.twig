{% extends 'base.html.twig' %}

{% block title %}Témoignages{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="bloc padding">
    	{% for testimony in testimonies %}
        <div class="temoignage" onclick="javascript:document.location.href='{{ path('specific_testimony', {'idTestimony': testimony.id}) }}'">
   					<p class='head'> {{ testimony.firstName }} {{ testimony.name }}, {{ testimony.age }} ans</p>
   					<p class="date">Fait le : {{ testimony.date|date("m/d/Y") }}</p>
        </div>
    	{% endfor %}
</div>
{% endblock %}
