{% extends 'base.html.twig' %}

{% block title %} Modifier point{% endblock %}

{% block body %}
<div class="bloc padding">
	<h1>Modifier point</h1>

	{{ include('admin/map/_form.html.twig', {'button_label': 'Enregistrer'}) }}

	<a href="{{ path("map_index") }}">Retour à la liste</a>
	<div class="button del" onclick="useModal('confirmDelete');">Supprimer</div>
	
	<div class="modal alert" id="confirmDelete" style="display:none">
        <div class="body">
            <div class="head">
                <h1>Voulez-vous vraiment supprimer le point ?</h1>
                <div class="close" onclick="this.parentNode.parentNode.parentNode.style.display = 'none';">
                    <div class="cross"></div>
                </div>
            </div>
            
            {{ include('admin/map/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}