<form method="post" action="{{ path('press_review_delete', {'id': press_review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ press_review.id) }}">
    <button class="btn">Delete</button>
</form>
